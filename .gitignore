# Dépendances
node_modules/

# Build
dist/
*.tsbuildinfo

# Graphe de contexte (graphify) — régénérable par `graphify update .`
graphify-out/

# Environnement — ne jamais committer de clé (Claude API, base, etc.)
.env
.env.*
!.env.example

# Logs
*.log
npm-debug.log*

# Réglages Claude Code propres à une machine — les hooks Impeccable y pointent
# vers des chemins absolus (/root/.claude/...) qui n'existent pas ailleurs.
.claude/settings.local.json

# Éditeurs & OS
.DS_Store
Thumbs.db
.vscode/*
!.vscode/extensions.json
.idea/
